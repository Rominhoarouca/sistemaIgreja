server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # ── Compressão ────────────────────────────────────────────────────────────
    # O `flutter build web` já emite .br e .gz ao lado de cada arquivo; estas
    # duas diretivas só mandam o nginx usá-los. brotli vem antes porque rende
    # bem mais no bundle (main.dart.js: 1,1 MB em br contra 1,6 MB em gzip).
    # Nenhuma compressão on-the-fly acontece para esses arquivos — nada de CPU
    # por requisição.
    brotli_static on;
    gzip_static on;

    # Fallback on-the-fly para o que não tem par pré-comprimido.
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript application/wasm image/svg+xml;
    gzip_min_length 1024;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 5;

    # Service worker — must never be cached (browser would serve stale SW)
    location = /flutter_service_worker.js {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        try_files $uri =404;
    }

    # Flutter bootstrap — no cache
    location = /flutter_bootstrap.js {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        try_files $uri =404;
    }

    # index.html — no cache so the app always fetches the latest version
    location = /index.html {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        try_files $uri =404;
    }

    # PWA manifest — short cache
    location = /manifest.json {
        add_header Cache-Control "max-age=3600";
        try_files $uri =404;
    }

    # Static assets (JS, CSS, fonts, images, WASM) — 1 year immutable
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|wasm)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        add_header Vary "Accept-Encoding";
        try_files $uri =404;
    }

    # SPA fallback — all unmatched routes serve index.html (GoRouter / History API)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
